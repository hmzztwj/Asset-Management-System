# 依赖锁定为精确版本：避免重建镜像时被自动升级到不兼容的大版本。
# 升级时请同步在本机验证（manage.py check + 关键流程回归）后再改这里。
Django==5.2.17
openpyxl==3.1.5
# 资产标签二维码（纯离线生成，输出 SVG，不依赖 Pillow）
qrcode==8.2

# ---- 生产部署（Docker 容器内使用，本机 runserver 不需要）----
gunicorn==23.0.0
whitenoise==6.8.2
# Redis 缓存驱动（设置 ASSETS_REDIS_URL 时使用，Django 5 内置 RedisCache 后端）
redis==5.2.1
